# ─────────────────────────────────────────────────────────
# Copy this file to .env and fill in your real values.
# NEVER commit .env to git.
# ─────────────────────────────────────────────────────────

# ── Database ─────────────────────────────────────────────
# For local development (MySQL):
# DATABASE_URL="mysql://root:PASSWORD@localhost:3306/pfe_db"
#
# For production on Vercel, use a cloud MySQL provider:
#   • Railway   → https://railway.app  (free tier available)
#   • PlanetScale→ https://planetscale.com
#   • Aiven      → https://aiven.io
# Example (Railway):
# DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/pfe_db"
DATABASE_URL="mysql://root:@localhost:3306/pfe_db"

# ── JWT ──────────────────────────────────────────────────
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET="CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET"
JWT_EXPIRES_IN="7d"

# ── App URL ───────────────────────────────────────────────
# For local development:
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
# For production, set this to your Vercel URL:
# NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
